extends ../layout
block content
    .container
        form.from-horizontal(method="post",action="/admin/movie/new")
            .form-group
                label.col-sm-2.control-label(for="inputFlash") 片源地址
                .col-sm-10
                    input#inputFlash.form-control(type="text")
            .from-group
                .col-sm-offset-2.col-sm-10
                    button.btn.btn-primary(type="submit") 录入